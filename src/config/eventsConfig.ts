/**
 * Raw JSON used at runtime so the app can load the latest scraped events without a new store build.
 * Default branch `main`; change if your default branch differs.
 */
export const EVENTS_JSON_RAW_URL =
  'https://raw.githubusercontent.com/robinjes/BillGlassMinistriesApp/main/assets/events.json';
