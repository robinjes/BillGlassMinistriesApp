/**
 * Raw JSON used at runtime so the app can load latest scraped positions
 * without requiring a new app-store build.
 */
export const POSITIONS_JSON_RAW_URL =
  'https://raw.githubusercontent.com/robinjes/BillGlassMinistriesApp/main/assets/positions.json';
